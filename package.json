{
  "name": "carbon-shield",
  "version": "1.0.0",
  "private": true,
  "description": "Blockchain-Based Micro Carbon Credit Platform",
  "scripts": {
    "compile": "cd contracts && npx hardhat compile",
    "deploy:mumbai": "cd contracts && npx hardhat run scripts/deploy.js --network mumbai",
    "frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd contracts && npm install && cd ../frontend && npm install"
  }
}
